<%= simple_form_for [@child, current_user, booking], url: url do |f| %>
  <%= f.error_notification %>

  <% @child.availabilities.each do |availability| %>
    <li>Available FROM: <%= availability.start_time %>
    TO: <%= availability.end_time %></li>
  <% end %>

  <%= f.input :start_time %>
  <%= f.input :end_time %>

  <%= f.button :submit %>
<% end %>
